package com.prox.deals.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Palette chosen for a grocery-deals product: a fresh "market green" as the
 * brand colour (signals fresh food + savings), a warm amber for the savings
 * accents and badges (price tags / "deal" energy), and soft neutral greys so
 * the green and amber stand out. We deliberately avoid pure black on white to
 * keep it feeling friendly rather than clinical.
 */

// Brand greens
val ProxGreen = Color(0xFF1E7D4F)
val ProxGreenDark = Color(0xFF14613C)
val ProxGreenLight = Color(0xFFD7EFE0)

// Savings accent (amber)
val ProxAmber = Color(0xFFE08A1E)
val ProxAmberLight = Color(0xFFFCEFD6)

// Neutrals
val ProxBackground = Color(0xFFF7F8F6)
val ProxSurface = Color(0xFFFFFFFF)
val ProxText = Color(0xFF1B1C1B)
val ProxTextMuted = Color(0xFF6B716D)
val ProxError = Color(0xFFB3261E)
